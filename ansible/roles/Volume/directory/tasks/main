---
- include_vars:
    file: "{{playbook_dir}}/roles/Volume/create/vars/main"



- include_tasks: if btrfs
  when: filesystem == "btrfs"

- include_tasks: if btrfs
  when: filesystem == "xfs"

- include_tasks: if zfs
  when: filesystem == "zfs"
